node_modules
node_modules.nosync
node_modules 2
node_modules 3
node_modules 4
dist
.DS_Store
*.tgz
.env
.env.local
coverage

# package-lock.json IS committed (SYS-2556): npm ci in CI needs it, and
# a library repo's lockfile never affects consumers — npm ignores
# dependency lockfiles on install. It must resolve from public npm only.
